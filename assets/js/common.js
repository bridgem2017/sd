
function initLayout(active=''){const map=[['index.html','홈'],['corrugated.html','골판지박스'],['colorbox.html','칼라박스'],['editor.html','전개도 에디터'],['quote.html','견적문의'],['support.html','고객지원']];const nav=document.getElementById('navLinks');if(nav){nav.innerHTML=map.map(([href,label])=>`<a href="${href}" ${href===active?'class="active"':''}>${label}</a>`).join('')}const m=document.getElementById('mobileLinks');if(m){m.innerHTML=map.map(([href,label])=>`<a href="${href}">${label}</a>`).join('')}const btn=document.getElementById('menuBtn');const drawer=document.getElementById('drawer');if(btn&&drawer){btn.onclick=()=>drawer.classList.add('open');drawer.querySelector('.overlay').onclick=()=>drawer.classList.remove('open')}}
